import getCookie from "~/assets/utils/get-cookie";

/**
 * 路由中间件
 * @param route 路由实例
 */
export default function ({app, store, route, redirect, req}) {
    // const {lang} = getCookie(req)
    // if (lang) {
    //     store.commit('setLang', lang)
    // }
    // redirect(route.path)
    // console.log(route);
    // if (route.fullPath == '/bbs') {
    //     return redirect('/s15/pc/bbs')
    // }
}
